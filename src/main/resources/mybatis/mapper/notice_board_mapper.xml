<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.heatokpia.mapper.NoticeBoardMapper">
	
	<insert id="save" parameterType="NoticeBoard">
		<![CDATA[
			INSERT INTO notice_board(SEQ, TITLE, CONTENT, CREATEDATE, ID)
			VALUES (
			seq_notice_board.NEXTVAL,
			#{title},
			#{content},
			SYSDATE,
			#{id}
			)
		]]>
	</insert>
	
	<select id="findTitleList" resultType="NoticeBoard">
		<![CDATA[
			SELECT seq, title, createdate
			FROM notice_board
		]]>
	</select>
	
</mapper>